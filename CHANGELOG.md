# Changelog

本项目的重要变更记录在此文件中。

## [Unreleased]

### Added

- XML 编辑模式，支持自动格式化、离线语法高亮、标签折叠、子元素计数和片段复制。
- XML 中文语法诊断，可定位标签不匹配、漏闭合标签、多根元素、非法属性与未转义字符。
- XML 模式及文本内容刷新恢复，大文件使用后台 Worker 格式化。
- JSON 与 XML 粘贴后自动格式化，并将编辑视图水平归位到最左侧。
- JSON、XML 与代码模式使用独立草稿；XML 解析为 JSON 时保留原 XML，并把结果写入 JSON 草稿。
- 修复切换到空白模式后仍残留上一模式折叠按钮的问题。
- 局部粘贴或复制行时保持原样，仅在空白面板或全选覆盖整份 JSON/XML 时自动格式化。
- 支持无选区时用 Ctrl/⌘+C、Ctrl/⌘+X 复制或剪切光标整行，并以整行方式粘贴；缩小行内折叠控件。

## [1.0.0] - 2026-09-03

### Added

- 离线 JSON 编辑、格式化、折叠、行号、隐藏 null 与中文语法诊断。
- 多语言代码高亮、查找替换和刷新恢复。
- 任意文本左右对比、差异统计、同步滚动与变更导航。
- 大文本虚拟渲染和 IndexedDB 压缩备份。

### Fixed

- 非 JSON 文本在隐藏 null 状态下也能直接进入代码模式。
